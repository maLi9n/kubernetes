apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefik-bouncer
  namespace: traefik
spec:
  plugin:
    bouncer:
      enabled: true
      crowdsecMode: stream
      crowdsecLapiScheme: https
      crowdsecLapiHost: crowdsec-service.crowdsec:8080
      crowdsecLapiKey: UVku/lPcndAdCjMlDMUjuCG2dWB5gul5qHxEFRfOWiI
